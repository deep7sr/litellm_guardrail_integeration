# Guardrail dependencies installed on top of the LiteLLM proxy image.
#
# The langchain pins matter: ragas 0.3.x imports modules that were removed in
# langchain-community 0.4 / langchain-core 1.x, and langchain-openai must be
# on its 0.3 line to coexist with openai 2.x (which litellm requires).
# This exact combination is verified working by the test suite.
ragas>=0.3.9,<0.4
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-openai>=0.3,<0.4
openai>=2.20,<3
asyncpg>=0.29.0
